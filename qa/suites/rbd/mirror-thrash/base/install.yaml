meta:
- desc: run two ceph clusters and install rbd-mirror
tasks:
- install:
    extra_packages: [rbd-mirror]
- cephadm:
    cluster: cluster1
- cephadm:
    cluster: cluster2
overrides:
  ceph:
    create_rbd_pool: true 
